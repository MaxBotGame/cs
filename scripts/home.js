const chooseGameText = document.getElementById("chooseGameText");
chooseGameText.innerText = language === "en" ? "" : ""
const helpButton = document.getElementById("helpButton");
helpButton.innerText = language === "en" ? "support" : "support"
